# ضع ملفات الفيديو المحلية هنا

Drop local MP4s here: `demo-1.mp4`, `demo-2.mp4` (referenced by the video hub playlist).
The custom player shows a styled notice while they are missing — no console errors, no layout shift.
YouTube items play regardless (youtube-nocookie embeds).
